// app/resultat/page.js
export default function ResultatPage() {
  return (
    <main style={{ padding: 24, fontFamily: "Montserrat, sans-serif" }}>
      <h1 style={{ marginBottom: 8 }}>Observatoire — Résultat (provisoire)</h1>
      <p style={{ color: "#3B3B3B" }}>
        Pour l’instant, cette page est un “container”. On va y brancher le calcul à partir
        de tes fichiers JSON.
      </p>

      <hr style={{ margin: "20px 0" }} />

      <p>
        ✅ Prochaine étape : on lit tes <code>questions.json</code> / <code>categories.json</code> /{" "}
        <code>animaux.json</code> et on calcule une appellation humoristique.
      </p>

      <p style={{ color: "#3B3B3B" }}>
        Dis-moi ensuite comment tu stockes/charges actuellement les JSON (dans le repo via un dossier
        comme <code>/data</code> ou en variables).
      </p>
    </main>
  );
}
